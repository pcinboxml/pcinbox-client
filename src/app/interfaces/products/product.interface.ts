export default interface ProductI {
  categoryId: string;
  createdAt: string;
  description: string;
  idProduct: string;
  image_url: string;
  name: string;
  price: string;
  providerId: string;
  stock: number;
  quantity: number;
}
